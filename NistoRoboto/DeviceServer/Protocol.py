from NistoRoboto.shared.utilities import listify
from math import ceil,sqrt

class Protocol:
    def __init__(self,name):
        self.app = None
        if name is None:
            self.name = 'Protocol'
        else:
            self.name = name
        
    def status(self):
        status = []
        return kw

    def execute(self,kw):
        pass




   
